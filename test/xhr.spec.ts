import { assert } from "chai";
import {
    XHR,
} from "../src/xhr";


describe("XHR", () => {

    beforeEach(() => {
    });

    describe("something", () => {
        it("something else", () => {
        });

    });

});
